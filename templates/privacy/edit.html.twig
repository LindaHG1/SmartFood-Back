{% extends 'base.html.twig' %}

{% block title %}Políticas de Privacidad{% endblock %}

{% block body %}
    <div class="content">
        <div class="actionsForm backBtn">
            <a href="{{ path('app_privacy_index') }}" class="btn btn-add"><i class="fa-solid fa-arrow-left me-2"></i> Volver</a>
        </div>
        <h1 class="titleSection"><i class="fa-regular fa-pen-to-square me-2"></i>Editar Políticas de Privacidad</h1>
        {{ include('privacy/_form.html.twig', {'button_label': 'Guardar'}) }}
        <div class="actionsForm">
            {{ include('privacy/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
