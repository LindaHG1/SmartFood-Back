{% extends 'base.html.twig' %}

{% block title %}Políticas de Privacidad{% endblock %}

{% block body %}
    <h1>Políticas de Privacidad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ privacy.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ privacy.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_privacy_index') }}">back to list</a>

    <a href="{{ path('app_privacy_edit', {'id': privacy.id}) }}">edit</a>

    {{ include('privacy/_delete_form.html.twig') }}
{% endblock %}
