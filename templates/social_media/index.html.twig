{% extends 'base.html.twig' %}

{% block title %}SocialMedia index{% endblock %}

{% block body %}
    <h1>SocialMedia index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nameicon</th>
                <th>Linkicon</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for social_media in social_media %}
            <tr>
                <td>{{ social_media.id }}</td>
                <td>{{ social_media.nameicon }}</td>
                <td>{{ social_media.linkicon }}</td>
                <td>
                    <a href="{{ path('app_social_media_show', {'id': social_media.id}) }}">show</a>
                    <a href="{{ path('app_social_media_edit', {'id': social_media.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_social_media_new') }}">Create new</a>
{% endblock %}
